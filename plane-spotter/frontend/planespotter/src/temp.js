import React from "react";
import axios from "axios";

function DisplaySightings({ user }) {
  //   async function getSightings() {
  //     const result = await axios.get(`http://localhost:3001/spotting/${user}`);
  //     console.log("result", result.data.success);
  //     return result;
  //   }
  //   const result = await getSightings();

  return <h4>Display sightings:</h4>;
}
export default DisplaySightings;
